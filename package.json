{
  "name": "@hesed/trello",
  "description": "CLI for Trello API interaction",
  "version": "1.0.0",
  "author": "Hesed",
  "bin": {
    "trello": "./bin/run.js"
  },
  "bugs": "https://github.com/hesedcasa/trello/issues",
  "dependencies": {
    "@hesed/plugin-lib": "^0.13.0",
    "@inquirer/prompts": "^8.2.0",
    "@oclif/core": "^5",
    "@toon-format/toon": "^4.1.1",
    "fs-extra": "^11.3.3",
    "proxy-from-env": "^2.1.0",
    "trello.js": "^2.2.0",
    "undici": "^7.29.1"
  },
  "devDependencies": {
    "@eslint/compat": "^2",
    "@oclif/prettier-config": "^0.2.1",
    "@oclif/test": "^5",
    "@types/chai": "^5",
    "@types/fs-extra": "^11.0.4",
    "@types/mocha": "^10",
    "@types/node": "^26",
    "@types/proxy-from-env": "^1.0.4",
    "@types/sinon": "^22.0.0",
    "c8": "^12.0.0",
    "chai": "^6",
    "eslint": "^10",
    "eslint-config-oclif": "^7",
    "eslint-config-prettier": "^10",
    "esmock": "^2.7.3",
    "mocha": "^11",
    "oclif": "^6",
    "prettier": "^3.8.1",
    "shx": "^0.4.0",
    "ts-node": "^10",
    "ts-prune": "^0.10.3",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "files": [
    "./bin",
    "./dist",
    "./oclif.manifest.json"
  ],
  "homepage": "https://github.com/hesedcasa/trello",
  "keywords": [
    "oclif"
  ],
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "type": "module",
  "oclif": {
    "bin": "trello",
    "dirname": "trello",
    "commands": "./dist/commands",
    "topicSeparator": " ",
    "topics": {
      "trello": {
        "description": "Commands for managing Trello workflows and operations"
      },
      "trello:board": {
        "description": "Get details, cards, lists and members for board"
      },
      "trello:card": {
        "description": "Perform various operations on card"
      },
      "trello:checklist": {
        "description": "Perform various operations on checklist"
      },
      "trello:comment": {
        "description": "Add, update or delete comment"
      },
      "trello:label": {
        "description": "Get, create, or delete label"
      },
      "trello:list": {
        "description": "Get, create or archive list, or get cards in list"
      }
    }
  },
  "repository": "hesedcasa/trello",
  "scripts": {
    "build": "shx rm -rf dist tsconfig.tsbuildinfo && tsc -b",
    "lint": "eslint",
    "postpack": "shx rm -f oclif.manifest.json",
    "posttest": "npm run lint",
    "prepack": "oclif manifest && oclif readme",
    "test": "mocha --forbid-only \"test/**/*.test.ts\" --ignore \"test/e2e/**\"",
    "test:coverage": "c8 npm test",
    "test:coverage:report": "c8 --reporter=html --reporter=text npm test",
    "test:e2e": "./scripts/e2e.sh",
    "e2e:mocha": "mocha --forbid-only --timeout 120000 \"test/e2e/**/*.e2e.test.ts\"",
    "e2e:sweep": "node --loader ts-node/esm --no-warnings scripts/sweep.ts",
    "version": "oclif readme && git add README.md",
    "find-deadcode": "ts-prune --ignore '(run|default)'",
    "format": "eslint --cache --fix --quiet . && prettier --write --cache .",
    "pre-commit": "npm run format && npm run find-deadcode"
  },
  "types": "dist/index.d.ts",
  "c8": {
    "include": [
      "src/**/*.ts"
    ],
    "exclude": [
      "src/**/*.d.ts",
      "test/**/*"
    ],
    "reporter": [
      "text",
      "lcov",
      "html"
    ],
    "all": true,
    "check-coverage": true,
    "lines": 50,
    "functions": 50,
    "branches": 50,
    "statements": 50
  }
}
